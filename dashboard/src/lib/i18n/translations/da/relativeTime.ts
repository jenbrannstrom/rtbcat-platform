import type { PartialTranslations } from '../../types';

const value: PartialTranslations['relativeTime'] = {
    justNow: 'Lige nu',
    minutesAgo: 'm siden',
    hoursAgo: 't siden',
    daysAgo: 'd siden',
  };

export default value;
