name: Deploy to AWS

on:
  push:
    branches:
      - unified-platform

# Prevent concurrent deployments - only one deploy can run at a time
# If a new deploy is triggered while one is running, the old one is cancelled
concurrency:
  group: production-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ec2-user
          key: ${{ secrets.AWS_SSH_KEY }}
          script: |
            sudo -u catscan bash -c '
              cd ~/rtbcat-platform
              git fetch origin
              git reset --hard origin/unified-platform
              docker compose -f docker-compose.production.yml build
              docker compose -f docker-compose.production.yml up -d
            '

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ec2-user
          key: ${{ secrets.AWS_SSH_KEY }}
          script: |
            sleep 10
            docker ps
            curl -s http://localhost:8000/health || echo "API not responding yet"
