# Python
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
.env

# Node
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Data files (local development only - production uses ~/.catscan/)
data/

# Documentation - NOTE: docs/ is tracked in git (removed from gitignore)
# Sensitive deployment info should use environment variables, not hardcoded in docs

# Terraform state and user-specific config
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/tfplan
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfvars
terraform/gcp/terraform.tfvars

# Deployment prompts with real infrastructure details (use .example as template)
prompts/deploy-catscan.md

# Note: docs/GCP_CREDENTIALS_SETUP.md is now tracked (contains setup instructions, not secrets)
investigations/

# Claude Code settings and agents (instance-specific)
.claude/
*.db
catscan-ci-key.json
Home page structure rtb.txt
Investigate and get to.txt
