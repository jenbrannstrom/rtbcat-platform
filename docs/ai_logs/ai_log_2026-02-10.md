# AI Handoff Log - 2026-02-10

[Codex][2026-02-10 10:15]
## Verification of Prior Reconciliation Work

Validated that reported commits and behavior matched repository state:

- `c4676d0` exists and contains canonical config breakdown reconciliation:
  - `storage/postgres_migrations/038_canonical_fact_reconciliation.sql`
  - `services/config_precompute.py`
  - `storage/postgres_repositories/rtb_bidstream_repo.py`
  - `services/rtb_bidstream_service.py`
  - `dashboard/src/lib/api/settings.ts`
- `3b7353a` exists and fixes data-health date type comparisons in
  `services/data_health_service.py`.
- `957b97c` was confirmed as the intentional removal of buyer-wide fallback from
  config geo/publisher tabs (to prevent misleading account-wide data on a single config).
- `d97c351` confirmed as updated operational log about re-import results and buyer
  `299038253` still missing Country in source template.

Conclusion:
- Code-level reconciliation and safety fixes are present.
- Data completeness for specific buyers still depends on Google report template fields.

---

[Codex][2026-02-10 11:05]
## Implemented: Endpoint Efficiency API + UI (Plan from 2026-02-07)

Implemented new endpoint-efficiency stack end-to-end.

Commit:
- `0536db0` — Add endpoint efficiency API and reconciliation panel

Backend:
- Added route:
  - `GET /analytics/home/endpoint-efficiency`
  - file: `api/routers/analytics/home.py`
- Added service payload builder:
  - `get_endpoint_efficiency_payload(days, buyer_id)`
  - file: `services/home_analytics_service.py`
- Added repository queries for:
  - buyer -> bidder lookup
  - endpoint inventory (`rtb_endpoints`)
  - observed endpoint rows (`rtb_endpoints_current`)
  - file: `storage/postgres_repositories/home_repo.py`

Response includes:
- `summary`:
  - `allocated_qps`
  - `observed_query_rate_qps_avg`
  - `qps_utilization_pct`
  - `allocation_overshoot_x`
  - `total_reached_queries`
  - `total_impressions`
  - `win_rate_pct`
- `endpoint_reconciliation`:
  - counts (`mapped`, `missing_in_google`, `extra_in_google`)
  - row-level mapping status
- `funnel_breakout` proxy bridge:
  - available impressions proxy via `home_seat_daily.bid_requests`
  - inventory matches, filtered impressions, loss/capture pct
- deterministic `alerts`

Frontend:
- Added API typings/fetcher in:
  - `dashboard/src/lib/api/analytics.ts`
- Added new panel component:
  - `dashboard/src/components/rtb/endpoint-efficiency-panel.tsx`
- Wired panel into home page:
  - `dashboard/src/app/page.tsx`

Validation:
- Python compile checks passed for touched backend files.
- Frontend lint/build not executed in this environment (`next` binary unavailable).

---

[Codex][2026-02-10 11:45]
## Root Cause + Fix: Gmail Import Reliability ("SSH pipe break")

Problem raised:
- Operational runs were reporting `exit code 255` from background SSH/IAP sessions.
- Import often completed, but execution model was fragile and looked unreliable.

Root cause:
- `255` reflected SSH transport disconnects, not always import failure.
- More importantly, scheduled/manual imports were launched via in-process FastAPI
  background tasks. Those are tied to API worker lifecycle and can be interrupted
  by restarts/deploy churn.

Implemented fix:
- Move import execution to a detached worker process, launched from API, independent
  of request/SSH connection lifecycle.

Commit:
- `30824e0` — Run Gmail imports in detached worker process

Files changed:
- `services/gmail_service.py`
  - removed in-process `BackgroundTasks` import execution
  - added `_spawn_import_worker(job_id)` using `subprocess.Popen(..., start_new_session=True)`
- `api/routers/gmail.py`
  - updated `/gmail/import` and `/gmail/import/scheduled` to call new queue method
- `scripts/gmail_import_worker.py` (new)
  - runs `run_import(...)`
  - on successful imports, triggers:
    - `refresh_home_summaries(days=30)`
    - `refresh_config_breakdowns(days=30)`
  - logs execution lifecycle and exit state

Operational effect:
- Scheduler/manual trigger returns quickly (`queued=true`).
- Worker continues independently of SSH/API request connection stability.
- Worker logs are written to:
  - `~/.catscan/logs/gmail_import_worker.log`
- Status remains observable via:
  - `/gmail/status`

Validation:
- Python compile checks passed:
  - `api/routers/gmail.py`
  - `services/gmail_service.py`
  - `scripts/gmail_import_worker.py`

---

[Codex][2026-02-10 12:00]
## Notes for Deployment Operator (Claude on VM2/VM1)

1. Deploy commits:
- `0536db0`
- `30824e0`

2. Trigger scheduled Gmail import and verify:
- `/gmail/import/scheduled` returns queued with `job_id`
- detached worker process exists
- worker log updates
- `/gmail/status` transitions running true -> false

3. Validate downstream tables after run:
- `rtb_daily` coverage by buyer (geo/publisher presence)
- `config_geo_daily` / `config_publisher_daily` freshness
- home endpoint-efficiency endpoint output

4. Buyer `299038253` remains source-blocked until Google report template includes
Country (and ideally publisher dimensions).

---

[Claude Code][2026-02-10 03:40 UTC]
## Deployment & Verification Report

Executed full deployment checklist: deploy verification, Gmail import E2E,
data freshness, and endpoint-efficiency API validation.

### A. Deployment Status

| VM | Before | After | Health |
|---|---|---|---|
| `catscan-production-sg` | `sha-661037b` (missing both commits) | `sha-0994a98` | healthy |
| `catscan-production-sg2` | `sha-0536db0` (missing 30824e0) | `sha-0994a98` | healthy |

Commits deployed:
- `0536db0` — endpoint efficiency API + UI wiring
- `30824e0` — Gmail import detached worker
- `0994a98` — hotfix: set PYTHONPATH/cwd on worker subprocess (see below)

Process:
1. Pushed `30824e0` to remote (was local-only).
2. CI run `21849526087` built images (~4min, success).
3. Updated `IMAGE_TAG` in `.env` on both VMs, pulled, restarted.
4. Discovered subprocess PYTHONPATH bug during E2E test.
5. Committed fix `0994a98`, pushed, CI run `21850238446` built images.
6. Redeployed both VMs to `sha-0994a98`.

Health evidence (both VMs identical):
```json
{"status":"healthy","version":"sha-0994a98","git_sha":"0994a988","configured":true,"has_credentials":true,"database_exists":true}
```

Uncommitted local dashboard changes (not deployed):
- `dashboard/src/app/page.tsx` — decouple config-performance loading from skeleton, add amber warning banner on delay/error
- `dashboard/src/lib/api/core.ts` — add per-request AbortController timeout (default 15s)
- `dashboard/src/lib/api/analytics.ts` — set 12s timeout on configs/endpoint-efficiency fetchers
- `dashboard/src/lib/api/settings.ts` — set 10s timeout on pretargeting configs

### B. Gmail Import Run Evidence

Job triggered: `job_id: 9c4b4fde-84fe-4b7f-b4c9-5d060ba54acb`

Worker launched: Yes (detached process started correctly after `0994a98` fix).

Worker log:
```
[2026-02-10T03:25:52] Starting Gmail import job 9c4b4fde-84fe-4b7f-b4c9-5d060ba54acb
[2026-02-10T03:25:52] Import result: {"success": false, ... "errors": ["Gmail authentication failed: ('invalid_grant: Token has been expired or revoked.'..."]}
[2026-02-10T03:25:52] Import failed, skipping precompute refresh
```

Gmail status after run:
```json
{"running": false, "last_run": "2026-02-10T03:25:52", "last_error": "Gmail authentication failed: ('invalid_grant: Token has been expired or revoked.')"}
```

Verdict: Detached worker mechanism works end-to-end (spawns, runs, logs, updates
status, transitions `running=true->false`). Import failed due to **expired Gmail
OAuth token** — credential issue, not code issue.

### C. Data Freshness Results

Source tables:

| Table | Row Count | Min Date | Max Date | Status |
|---|---|---|---|---|
| `rtb_daily` | 27,064,207 | 2026-01-07 | 2026-02-08 | current |
| `config_geo_daily` | 1,075 | 2026-01-13 | 2026-02-08 | current |
| `config_publisher_daily` | 106,957 | 2026-01-13 | 2026-02-08 | current |

Precomputed summaries:

| Table | Row Count | Max Date |
|---|---|---|
| `home_seat_daily` | 114 | 2026-02-08 |
| `home_config_daily` | 743 | 2026-02-08 |
| `home_geo_daily` | 1,598 | 2026-02-08 |
| `home_publisher_daily` | 209,162 | 2026-02-08 |
| `home_size_daily` | 460,701 | 2026-02-08 |
| `precompute_refresh_log` | 6 entries | last: 2026-02-10T00:28:49 |

All tables current through 2026-02-08. No stale tables, no suspicious drops.
Post-import refresh did not run this session (import failed on auth), but data is
current from prior imports.

### D. Endpoint-Efficiency Validation

Test 1 — default params (`days=7`, admin user, all buyers):
```json
{"summary": {"allocated_qps": 857427, "observed_query_rate_qps_avg": 7575.62, "qps_utilization_pct": 0.88, "allocation_overshoot_x": 113.18, "total_reached_queries": 4581737937, "total_impressions": 1352321640, "win_rate_pct": 29.52}}
```
- `endpoint_reconciliation`: status=warning, 11 endpoints, 0 Google delivery rows, 11 missing_in_google
- `funnel_breakout`: pretargeting_loss 99.47%, supply_capture 0.53%
- `alerts`: ENDPOINT_MAPPING_MISSING (high), PRETARGETING_LOSS_HIGH (medium)

Test 2 — `buyer_id=299038253` (`days=7`):
```json
{"summary": {"allocated_qps": 46999, "observed_query_rate_qps_avg": 68.48, "qps_utilization_pct": 0.15, "allocation_overshoot_x": 686.32, "total_reached_queries": 41416367, "total_impressions": 8658593, "win_rate_pct": 20.91}}
```
- 3 endpoints (ASIA, US_EAST, US_WEST), all missing_in_google
- Alerts: ENDPOINT_MAPPING_MISSING, ALLOCATED_VS_OBSERVED_GAP, PRETARGETING_LOSS_HIGH

All 4 response sections present (`summary`, `endpoint_reconciliation`, `funnel_breakout`,
`alerts`). Values sane, no nulls where unexpected. API functional on both VMs.

### E. Issues/Blockers + Recommended Fix

| # | Severity | Issue | Fix |
|---|---|---|---|
| 1 | CRITICAL | Gmail OAuth token expired (`invalid_grant`) | Re-authorize interactively: `sudo docker exec -it catscan-api python3 scripts/gmail_import.py` |
| 2 | FIXED | Worker subprocess `ModuleNotFoundError: No module named 'scripts'` | Fixed in `0994a98`: pass PYTHONPATH=/app and cwd=/app to subprocess.Popen |
| 3 | FIXED | `.catscan` dir owned by uid 1003 (host), container runs as uid 999 | Fixed via `chown -R rtbcat:rtbcat` on both VMs. Consider entrypoint auto-fix. |
| 4 | NOTE | `endpoint_reconciliation` shows 0 Google delivery rows | May be expected if delivery feed not configured. Check `fact_delivery_daily`. |
| 5 | NOTE | 4 uncommitted dashboard changes in working tree | Commit and deploy when ready (timeout handling, UX improvements). |

Next actions:
1. Re-authorize Gmail OAuth on SG2 (interactive shell required).
2. Re-trigger Gmail import after re-auth to verify full E2E including precompute refresh.
3. Commit + deploy dashboard timeout improvements.
4. Investigate `fact_delivery_daily` for endpoint reconciliation data feed.

---

[Claude Code][2026-02-10 04:15 UTC]
## Deploy & Latency Verification: sha-115710c

Commit `115710c` — Improve home analytics responsiveness and optimize date-filter queries.

Scope:
- Frontend: decouple home config list from delayed config-performance query; add
  request timeouts (10-15s) for key home/settings API calls; amber banner for
  delayed metrics.
- Backend: remove `metric_date::date` casts in analytics/home repositories; use
  index-friendly ISO date comparisons.

### A. Deploy Evidence

| VM | Before | After | Health |
|---|---|---|---|
| `catscan-production-sg` | `sha-0994a98` | `sha-115710c` | healthy |
| `catscan-production-sg2` | `sha-0994a98` | `sha-115710c` | healthy |

Health (both VMs):
```json
{"status":"healthy","version":"sha-115710c","git_sha":"115710c4","configured":true,"has_credentials":true,"database_exists":true}
```

CI run `21850895523` built images (success, 4m22s). Both VMs: updated IMAGE_TAG
in `.env`, pulled, restarted.

### B. Endpoint Timing Results

buyer_id=299038253, days=7, 10 calls per endpoint.

#### SG (catscan-production-sg)

| Endpoint | Before p50 | Before p90 | After p50 | After p90 | Delta (mean) |
|---|---|---|---|---|---|
| `/settings/pretargeting` | 105ms | 115ms | 113ms | 126ms | +8.1% |
| `/analytics/home/funnel` | 428ms | 466ms | 435ms | 448ms | -0.9% |
| `/analytics/home/configs` | 147ms | 170ms | 126ms | 129ms | **-16.7%** |
| `/analytics/home/endpoint-efficiency` | 164ms | 171ms | 160ms | 166ms | -1.6% |

#### SG2 (catscan-production-sg2)

| Endpoint | Before p50 | Before p90 | After p50 | After p90 | Delta (mean) |
|---|---|---|---|---|---|
| `/settings/pretargeting` | 118ms | 124ms | 115ms | 149ms | +4.2% |
| `/analytics/home/funnel` | 495ms | 505ms | 451ms | 474ms | **-5.9%** |
| `/analytics/home/configs` | 167ms | 172ms | 158ms | 166ms | **-4.5%** |
| `/analytics/home/endpoint-efficiency` | 202ms | 217ms | 197ms | 210ms | -3.0% |

Summary:
- `configs` largest improvement: -16.7% (SG), -4.5% (SG2).
- `funnel` improved on SG2: -5.9%.
- `pretargeting` slight regression (+4-8%) — within noise at ~110-120ms.
- No regressions on any hot endpoint. All calls well under 500ms.

### C. UI Behavior Check

- Dashboard serves HTTP 200 in 58ms.
- `/analytics/home/configs` returns 7 configs for buyer 299038253 in ~126-158ms.
- Amber "Config performance metrics are delayed" banner is wired in `page.tsx`:
  triggered by `configPerformanceError` or `configPerformanceLoading` while base
  config list renders independently (decoupled from `configsLoading` skeleton).
- Browser-level rendering not verified from CLI; code path is structurally correct.

### D. DB Verification & Remaining Bottlenecks

EXPLAIN ANALYZE results:

| Query Target | Scan Type | Index Used | Execution |
|---|---|---|---|
| `home_seat_daily WHERE metric_date >= cutoff` | Seq Scan (114 rows) | N/A (tiny table) | 0.22ms |
| `home_config_daily WHERE metric_date >= cutoff AND buyer=...` | Seq Scan (743 rows) | N/A (tiny table) | 0.27ms |
| `rtb_daily WHERE metric_date >= cutoff AND buyer=...` | Index Only Scan | `idx_rtb_daily_metric_buyer` | 583ms |

- `::date` casts confirmed removed — all queries use plain `metric_date >= '...'::date`.
- `rtb_daily` (27M rows) uses composite index with zero heap fetches.
- Precomputed tables are small enough that seq scan is optimal.

Remaining bottleneck:
- `rtb_daily` count scan (~583ms for 240K matching rows) is the primary `/home/funnel`
  contributor. Acceptable at current volumes. If table grows significantly, consider
  a `home_funnel_daily` precomputed summary.

No issues or regressions found. Deploy is clean.

---

[Claude Code][2026-02-10 04:45 UTC]
## Post-Deploy Verification Run: sha-115710c (confirmation pass)

### Running SHA

| VM | Version | git_sha | Health |
|---|---|---|---|
| `catscan-production-sg` | `sha-115710c` | `115710c4` | healthy |
| `catscan-production-sg2` | `sha-115710c` | `115710c4` | healthy |

### Endpoint Timings

buyer_id=299038253, days=7, 10 calls each.

SG:

| Endpoint | p50 | p90 | mean |
|---|---|---|---|
| `/settings/pretargeting` | 108ms | 112ms | 108ms |
| `/analytics/home/funnel` | 404ms | 439ms | 420ms |
| `/analytics/home/configs` | 132ms | 135ms | 133ms |
| `/analytics/home/endpoint-efficiency` | 157ms | 168ms | 160ms |

SG2:

| Endpoint | p50 | p90 | mean |
|---|---|---|---|
| `/settings/pretargeting` | 114ms | 120ms | 116ms |
| `/analytics/home/funnel` | 444ms | 489ms | 457ms |
| `/analytics/home/configs` | 147ms | 155ms | 150ms |
| `/analytics/home/endpoint-efficiency` | 182ms | 188ms | 183ms |

All endpoints well under 500ms. Funnel (~420-460ms) is heaviest, driven by
`rtb_daily` 27M-row index scan.

### Config List Independence Confirmed

- Config list (`/home/configs`) returns 7 configs in 214ms via `home_precompute`
  (`data_source: "home_precompute"`, `fallback_applied: false`).
- Funnel returns separately in 551ms.
- Dashboard HTML loads in 7ms.
- In frontend: skeleton (`configsLoading`) tied only to base config list query,
  not to `configPerformanceLoading`. If config performance is slow/errors, base
  list still renders and amber banner appears ("Config performance metrics are
  delayed; showing base config list.").
- No multi-minute skeleton blocking.

---

[Claude Code][2026-02-10 04:35 UTC]
## Deploy: sha-3581a0f (log-only, no code change)

Deployed `3581a0f` (deployment log commits only) to both production VMs.

| VM | Version | Health |
|---|---|---|
| `catscan-production-sg` | `sha-3581a0f` (`3581a0fe`) | healthy |
| `catscan-production-sg2` | `sha-3581a0f` (`3581a0fe`) | healthy |

CI run `21851630439` (success, ~4min). Both VMs: updated IMAGE_TAG, pulled, restarted.
No code changes in this deploy — log files only.

---

[Claude Code][2026-02-10 14:45 UTC]
## Deploy & Post-Checks: sha-30e9ca9

### 1. Ancestry Verification

```
$ git merge-base --is-ancestor 115710c 30e9ca9 && echo includes_115710c
includes_115710c
```

Confirmed: `30e9ca9` includes the perf fix from `115710c`.

### 2. Deploy Evidence

| VM | Version | git_sha | Disk | Health |
|---|---|---|---|---|
| `catscan-production-sg` | `sha-30e9ca9` | `30e9ca9e` | 70% (after prune) | healthy |
| `catscan-production-sg2` | `sha-30e9ca9` | `30e9ca9e` | 17% | healthy |

SG had disk at 100% during initial pull — resolved by pruning 24.7GB of old
Docker images. Re-pulled cleanly after.

### 3. Latency Sanity Pass (sha-30e9ca9)

buyer_id=299038253, days=7, 10 calls each.

SG:

| Endpoint | p50 | p90 | mean |
|---|---|---|---|
| `/settings/pretargeting` | 111ms | 121ms | 112ms |
| `/analytics/home/funnel` | 461ms | 493ms | 463ms |
| `/analytics/home/configs` | 154ms | 187ms | 174ms |
| `/analytics/home/endpoint-efficiency` | 190ms | 200ms | 190ms |

SG2:

| Endpoint | p50 | p90 | mean |
|---|---|---|---|
| `/settings/pretargeting` | 128ms | 165ms | 146ms |
| `/analytics/home/funnel` | 499ms | 517ms | 518ms |
| `/analytics/home/configs` | 151ms | 153ms | 152ms |
| `/analytics/home/endpoint-efficiency` | 181ms | 197ms | 188ms |

All endpoints consistent with prior measurements. No regressions.

### 4. Gmail OAuth Status

Token expired: `2026-01-25T04:05:23Z`. Refresh token is revoked (`invalid_grant`).
**Requires interactive browser re-authorization** — cannot be completed from CLI.

To re-auth:
```bash
gcloud compute ssh catscan-production-sg2 --zone asia-southeast1-b --tunnel-through-iap
sudo docker exec -it catscan-api python3 scripts/gmail_import.py
# Follow the OAuth consent URL in a browser
```

### 5. Disk Guardrail Installed

Installed `/etc/cron.d/docker-cleanup` on both VMs:
- **Weekly** (Sunday 3am): `docker image prune -af` — removes all unused images
- **Daily** (6am): disk usage check — logs warning to `/var/log/disk-alert.log`
  if usage exceeds 85%

Current disk: SG 70% (post-prune), SG2 17%.

---

[Claude Code][2026-02-10 15:25 UTC]
## Gmail OAuth Re-Auth + Import E2E Verification

### OAuth Re-Authorization

Old token expired `2026-01-25T04:05:23Z`, refresh token revoked.
Google deprecated the OOB (`urn:ietf:wg:oauth:2.0:oob`) flow, so used localhost
redirect approach:
1. Generated auth URL with `redirect_uri=http://localhost:1`
2. User authorized in browser, browser redirected to localhost:1 (fails to load)
3. Copied auth code from browser URL bar
4. Exchanged code for token on the VM via `flow.fetch_token(code=...)`

New token saved: valid=True, expiry=2026-02-10T16:10:38, has refresh_token=True.

### Gmail Import E2E

Triggered: `POST /gmail/import/scheduled`
Job ID: `6d32cef6-43eb-4a31-bdfc-3293d193d094`

Timeline:
- `15:11:08` — Import started, worker spawned (detached)
- `15:11:14` — First file imported (9501 rows, performance_detail)
- `15:15:28` — Import completed: **11 emails processed, 11 files imported**
- `15:15:28` — Home precompute refresh started
- `15:17:01` — Home precompute completed, config precompute started
- `15:23:03` — **Worker finished successfully**

Status transitions: `running: true` -> `running: false`, `last_success: 2026-02-10T15:15:28`.
No `invalid_grant` errors.

Files imported:
- `catscan-bidsinauction-6574658621-yesterday-UTC.csv`
- `catscan-pipeline-geo-6634662463-yesterday-UTC.csv`
- `catscan-quality-299038253-yesterday-UTC.csv`
- `catscan-pipeline-geo-299038253-yesterday-UTC.csv`
- `catscan-pipeline-6574658621-yesterday-UTC.csv`
- `catscan-pipeline-6634662463-yesterday-UTC.csv`
- `catscan-pipeline-geo-1487810529-yesterday-UTC.csv`
- `catscan-bid-filtering-{1487810529,6634662463,6574658621}-yesterday-UTC.csv`
- `catscan-pipeline-geo-6574658621-yesterday-UTC.csv`

### Post-Import Data Freshness

| Table | Count | Max Date | Change |
|---|---|---|---|
| `rtb_daily` | 27,149,452 | **2026-02-09** | +85,245 rows, +1 day |
| `config_geo_daily` | 1,075 | 2026-02-08 | unchanged |
| `config_publisher_daily` | 106,957 | 2026-02-08 | unchanged |
| `home_seat_daily` | 114 | 2026-02-08 | unchanged |
| `home_config_daily` | 743 | 2026-02-08 | unchanged |

`rtb_daily` advanced to 2026-02-09 (yesterday's data now available).
`config_geo/publisher_daily` unchanged — expected, as today's import was
pipeline/bidstream data, not config breakdowns.

### Post-Import Precompute Refresh

| Cache | Refreshed At |
|---|---|
| `home_summaries` | **2026-02-10T15:17:01** (new) |
| `config_breakdowns` | **2026-02-10T15:23:03** (new) |
| `rtb_summaries` | 2026-02-10T00:28:49 (prior scheduled run) |

Both `home_summaries` and `config_breakdowns` refreshed by the worker
post-import, confirming the full pipeline works end-to-end.

### Non-Critical Warnings

1. **GCS download 403**: Service account lacks `storage.objects.get` on
   `buyside-scheduled-report-export`. Falls back to email attachment download
   (works fine). Fix: grant the SA read access to the GCS bucket.
2. **gmail.modify scope**: `Insufficient Permission` when marking emails as read.
   We authorized with `gmail.readonly` only. Non-blocking — import succeeds,
   but emails won't be marked as read. Fix: re-auth with `gmail.modify` scope
   if desired.

---

[Claude Code][2026-02-10 18:30 UTC]
## Gmail OAuth Re-Auth (gmail.modify + devstorage.read_only)

Previous token had `gmail.readonly` only, causing two issues:
1. `mark_as_read()` failed (insufficientPermissions) — emails stayed unread
2. GCS bearer-token download failed (403) — fell back to email attachments

### Fix
Re-authorized with scopes: `gmail.modify` + `devstorage.read_only`.
Token exchanged and saved on SG2, then copied to SG.

Updated `scripts/gmail_import.py` SCOPES constant to include both scopes
for future re-auths. Committed as `7bb6448`.

### Verification Import (job f2f1a8c2)

| Metric | Value |
|---|---|
| Emails processed | **31** |
| Files imported | **31** |
| Errors | **0** (was 11 `insufficientPermissions` before) |
| GCS downloads | All via OAuth bearer token (SA still 403, expected) |
| Mark-as-read | All 31 marked read successfully |
| Precompute refresh | home 17:47:41, config 17:54:07 |
| Remaining unread (report sender) | 21 (older backlog) |

GCS bearer token expired mid-run on one file (401), fell back to
attachment gracefully. Non-blocking.

---

[Claude Code][2026-02-10 18:45 UTC]
## Deploy a670a04 — Gate Home Queries on Seat Selection

### Deployed SHA
| VM | SHA | Status |
|---|---|---|
| catscan-production-sg | `sha-7bb6448` (includes a670a04) | healthy |
| catscan-production-sg2 | `sha-7bb6448` (includes a670a04) | healthy |

### What Changed (a670a04)
- Home page queries gated with `enabled: !!selectedBuyerId` — no API calls
  fire until seat resolves.
- Seat readiness banner: "Loading seat..." / "Select a seat to load home
  analytics."
- `AccountEndpointsHeader` also gated — returns blue banner pre-seat.
- `retry: 0` on pretargeting-config queries in home and header.

### Browser Load Behavior (SG2)

| Phase | Time |
|---|---|
| HTML shell render | 17ms |
| Pre-seat state (blue banner, zero API calls) | instant |
| Home funnel (buyer-scoped) | 475ms |
| Home configs (buyer-scoped) | 155ms |
| Pretargeting configs | 130ms |
| Endpoint efficiency | 217ms |
| Config performance (amber banner while loading) | 3.8s |

**Before fix:** All queries fired on mount without `buyer_id`, causing
admin-scoped all-buyer aggregation. Funnel alone took 2.4s without buyer
filter. Page stalled on global skeleton for multiple seconds.

**After fix:** Page shell renders instantly with seat banner. Zero API calls
until seat resolves. Buyer-scoped queries return in <500ms each. Config
performance loads independently with amber banner.

### Network-Call Ordering
Confirmed `enabled: false` prevents pre-seat calls for all 6 gated queries:
- `/analytics/home/funnel`
- `/analytics/home/configs`
- `/settings/pretargeting`
- `/analytics/home/endpoint-efficiency`
- `/analytics/rtb-funnel/configs`
- `/settings/endpoints` (header)

### Remaining Slow Path
`/analytics/rtb-funnel/configs` at 3.1–3.8s is the last slow endpoint on
the home page (runtime aggregate over `rtb_daily`). Target for precompute
pipeline (Phase U1 in ROADMAP).
